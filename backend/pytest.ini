[pytest]
# `pythonpath = .` lets tests do `from app.main import app` without
# installing the project as a package.
pythonpath = .
testpaths = tests
# -q  : quiet output
# -ra : show a summary of anything that wasn't a plain pass
addopts = -q -ra
